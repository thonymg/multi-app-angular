import { SharedComponentModule } from './shared-component.module';

describe('SharedComponentModule', () => {
  it('should work', () => {
    expect(new SharedComponentModule()).toBeDefined();
  });
});
